[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["cv-layer"]

[tool.uv.sources]
cv-layer = { workspace = true }

[dependency-groups]
dev = [
    "uvicorn>=0.41.0",
]
